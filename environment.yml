# Conda environment to enable code checking in vscode. Does not have gpu support.
name: groundgan
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - -r requirements.txt
      - --extra-index-url https://download.pytorch.org/whl/cpu torch==2.9.1
